import { StyleSheet, Platform, StatusBar } from "react-native";

export const styles = StyleSheet.create({
  container: {
    display: "flex",
    flex:1
  },
  topBanner: {
    flex: 2,
    minHeight: 80
  },
  listBanner: {
    flex: 10
  }
});
